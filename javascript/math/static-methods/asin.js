/**
 * O método estático asin retorna o arco seno de um número. O retorno é dado em
 * radianos.
 * 
 * Se o parâmetro passado não estiver entre -1 e 1 o método retornará NaN.
 */

console.log(Math.asin(1)); // (PI / 2)
 