/**
 * O método estático random retorna um número entre 0 e 1 (exclusivo).
 */

console.log(Math.random());

/* ---------------- gerando números aleatórios entre 0 e 100 ---------------- */

const x = Math.floor((Math.random() * 100) + 1);

console.log(x);
