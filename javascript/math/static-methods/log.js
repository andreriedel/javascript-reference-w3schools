/**
 * O método estático log retorna o logaritmo natural (base e) de um número.
 * 
 * Se o parâmetro passado for negativo o método retornará NaN.
 * Se o parâmetro for 0 o método retornará -Infinity.
 */

console.log(Math.log(5));
console.log(Math.log(0));
