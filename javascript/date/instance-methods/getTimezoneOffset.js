/**
 * o método getTimezoneOffset retorna a diferença em minutos do fuso horário
 * do interpretador para o fuso horário do padrão universal (UTC)
 */

const date = new Date();
console.log(date.getTimezoneOffset());
